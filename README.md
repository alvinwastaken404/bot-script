# bot-script

install nodejs & npm
  sudo apt update && sudo apt upgrade
  sudo apt install nodejs npm

make directory
  sudo mkdir /example
  cd /example
  npm init -y

run the script
  sudo node example.js

open your browser, and type <IP Address (local)>:3000
scan the QR code from Whatsapp > Linked devices
